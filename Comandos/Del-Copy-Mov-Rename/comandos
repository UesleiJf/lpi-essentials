-Manipulando arquivos

Removendo arquivos/diretórios

    rm arquivo (Remove o arquivo)

    rm -r dir (remove o diretório e seu conteúdo recursivamente)

    rm -f arquivo (remove forçadamente o diretório)

Copiando arquivos/diretórios

    cp arquivo destino (copia o arquivo para o destino especificado)

    cp arquivo1 arquivo2 arquivo3 destino (copia vários arquivos para um destino)

    cp -i arquivo destino (copia no modo interativo)

    cp -r diretorio destino (copia recursivamente o diretório)

   
Movendo arquivos

    mv arquivo arquivoNovo (renomeia arquivo ou diretórios)

    mv arquivo destino (move o arquivo ou diretório para o destino desejado)

    mv -i arquivo destino (move o arquivo para o destino, e se o destino já existir, é perguntado se deseja substituí-lo)

Comandos

    sort file (ordena em ordem crescente as linhas do arquivo)

    sort -r file (ordena em ordem descrescente as linhas do arquivo)

    tar c (Cria um arquivo)

    tar x (Extrai arquivos)

    tar t (mostra a lista de arquivos)

    tar v (Verboso) 

    tar z (Usa compressão)
